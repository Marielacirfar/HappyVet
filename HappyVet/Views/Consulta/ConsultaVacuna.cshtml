@model HappyVet.Models.Consulta
@Html.EditorFor(model => model.Vacuna)
@* @for (int i = 0; i < Model.Vacuna.Count; i++)
{
    <div class="form-group">
        @Html.DropDownListFor(model => model.Vacuna[i].VacunaRefId, ViewData["VacunaRefId"] as SelectList, "Seleccione una Vacuna", new { @class = "form-control" })
    </div>
} *@